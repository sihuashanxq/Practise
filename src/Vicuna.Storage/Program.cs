using System;

namespace Vicuna.Storage
{
    public class Program
    {
        public static void Main()
        {
            var byteString = new ByteString(BitConverter.GetBytes(1024));
            var stop=new System.Diagnostics.Stopwatch();
        }
    }
}
